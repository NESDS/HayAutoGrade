# Основные зависимости проекта HayAutoGrade

# Работа с данными
pandas>=2.0.0

# Telegram бот
aiogram>=3.0.0

# HTTP запросы (для LLM сервиса)
requests>=2.28.0

# Дополнительно для Excel файлов (используется pandas)
openpyxl>=3.0.0

# Загрузка переменных окружения из .env файла
python-dotenv>=1.0.0

# COM интерфейс для работы с Excel (Windows)
pywin32>=306

# СберЧат (корпоративный SDK)
# Пакет устанавливается из внутреннего PyPI/репозитория
# Импорт в коде: dialog_bot_sdk
# sber-sberchat-bot-sdk